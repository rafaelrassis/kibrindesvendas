# Ki Brindes Vendas

Repositório do projeto **Ki Brindes Vendas**.

## Status

Repositório recém-criado. Este é o commit inicial: contém apenas a estrutura
básica (README e `.gitignore`). A stack, a arquitetura e o código da aplicação
ainda serão definidos.

## Como começar

```bash
git clone https://github.com/rafaelrassis/kibrindesvendas.git
cd kibrindesvendas
```

## Estrutura

```
.
├── .gitignore    # arquivos ignorados pelo Git
└── README.md     # este arquivo
```

## Próximos passos

- [ ] Definir a stack do projeto (linguagem, framework, banco de dados)
- [ ] Criar o scaffold da aplicação
- [ ] Configurar ambiente de desenvolvimento (dependências, variáveis de ambiente)
- [ ] Configurar testes e integração contínua

## Convenções

- Branch principal: `main`
- Branches de trabalho: `feat/...`, `fix/...`, `chore/...`
- Segredos e credenciais **nunca** devem ser commitados — use arquivos `.env`
  locais (já ignorados pelo `.gitignore`).
